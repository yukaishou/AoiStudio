命令列表
命令                作用
add       添加对象（角色、背景、标记 flag）
move      移动角色，支持缓动动画
animation 执行角色动画（淡入淡出、缩放、抖动、跳跃、闪烁）
switch    切换背景、BGM、角色立绘
affection 角色好感度控制
wait      等待若干秒（非阻塞）
jump      剧本跳转，跳转外部剧本 / 当前剧本指定行
quit      退出程序

